import { MAP_ROTATION_DEG } from '../config/layout';

/** Rotation (radians) to give a game object so it stands upright on the rotated map. Use as `-UPRIGHT`. */
export const UPRIGHT = (MAP_ROTATION_DEG * Math.PI) / 180;
